import type React from "react"
import type { Metadata } from "next"
import { Inter } from "next/font/google"
import "./globals.css"

const inter = Inter({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "WheelTech Pro - Custom Wheels & Tires",
  description:
    "Premium custom wheels and tires for your vehicle. Professional installation and unbeatable quality guaranteed.",
  keywords: "wheels, tires, custom wheels, auto parts, car accessories",
    generator: 'v0.dev'
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}
